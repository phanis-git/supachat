version: "3"

services:
  backend:
    build: ./backend
    image: supachat-backend:latest
    container_name: backend
    expose:
      - "8000"
    env_file:
    - .env
    restart: always
    dns:
      - 8.8.8.8
      - 1.1.1.1

  frontend:
    build: ./frontend
    image: supachat-frontend:latest
    container_name: frontend
    expose:
      - "80"
    restart: always
    dns:
      - 8.8.8.8
      - 1.1.1.1

  nginx:
    image: nginx:alpine
    container_name: nginx
    ports:
      - "80:80"
    restart: always
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - frontend
      - backend

  prometheus:
    image: prom/prometheus
    container_name: prometheus
    restart: always
    ports:
      - "9090:9090"
    volumes:
      - ./monitoring/prometheus.yml:/etc/prometheus/prometheus.yml

  grafana:
    image: grafana/grafana:10.4.2
    container_name: grafana
    restart: always
    ports:
      - "3002:3000"

  devops-agent:
    build: ./devops-agent
    container_name: devops-agent
    restart: always
    ports:
      - "8001:8001"
    env_file:
      - .env
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock  # ← allows agent to control Docker
      - /home/ubuntu/supachat:/home/ubuntu/supachat
    depends_on:
      - backend
      - frontend