version: "3"

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    dns:
      - 8.8.8.8
      - 1.1.1.1

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
      # - "3000:80"
    # command: npm run dev -- --host 0.0.0.0
    environment:
    - HOST=0.0.0.0
    